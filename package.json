{
	"name": "bun-storage",
	"version": "1.1.2",
	"description": "A ponyfill for the Storage API, utilizing SQLite",
	"exports": "./index.ts",
	"type": "module",
	"engines": {
		"bun": ">=1.0.0",
		"node": ">=20"
	},
	"files": [
		"index.ts",
		"LICENSE",
		"README.md"
	],
	"scripts": {
		"check": "biome check --no-errors-on-unmatched --vcs-enabled=true --vcs-use-ignore-file=true",
		"check:ci": "biome ci",
		"dev": "bun run build -- --watch",
		"prepare": "lefthook install",
		"publish": "np --any-branch",
		"test": "bun test"
	},
	"keywords": [
		"bun",
		"storage api",
		"localstorage",
		"sessionstorage",
		"ponyfill"
	],
	"author": "Jan T. Sott",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/idleberg/bun-storage.git"
	},
	"devDependencies": {
		"@commitlint/cli": "^19.8.1",
		"@commitlint/config-conventional": "^19.8.1",
		"@idleberg/configs": "^0.3.1",
		"@types/node": "^24.5.2",
		"bun-types": "^1.2.22",
		"np": "^10.2.0"
	}
}
